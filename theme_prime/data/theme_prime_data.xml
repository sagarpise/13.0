<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data noupdate="1">
        <!-- FIXME: Should update priority only for theme's view -->
        <function model="ir.ui.view" name="write">
            <value model="ir.ui.view" eval="obj().with_context(active_test=False).search([('key','=', 'website_sale.products_categories')])[0].id"/>
            <value eval="{'priority': 15}"/>
        </function>
    </data>

</odoo>
